# Web application
Flask==2.3.3
gunicorn==23.0.0

# Primary disease detector
# The deployed model is an ONNX file and runs directly in ONNX Runtime.  Do
# not install PyTorch, Ultralytics, or timm here: their large dependency tree
# can exceed a Render instance's memory during startup.
onnx==1.16.2
onnxruntime==1.20.1

# Image handling
opencv-python-headless==4.8.0.74
Pillow==10.0.0
numpy==1.26.4
# Used to generate downloadable detection-report charts.
matplotlib==3.7.2

# Utilities
python-dotenv==1.0.0
requests==2.31.0
